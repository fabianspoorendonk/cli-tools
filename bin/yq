#!/usr/bin/env bash

# Command configuration
command_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"

# Container configuration
container_image="docker.io/mikefarah/yq@sha256:11a1f0b604b13dbbdc662260d8db6f644b22d8553122a25c1b5b2e8713ca6977" # 4.53.3

# Container run options
container_run_options=()
container_run_options+=(--interactive) # yq reads standard input when no file is provided.

# Container command arguments
container_command_args=("$@")

# Run container
# shellcheck source=lib/container.sh
source "$command_dir/../lib/container.sh"
container_run "${container_run_options[@]}" "$container_image" "${container_command_args[@]}"
